<form method="post" class="deleteForm" action="{{ path('opinion_delete', {'id': opinion.id}) }}" onsubmit="return confirm('Estas seguro de querer borrar esta opinion?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ opinion.id) }}">
    <button class="btn">Borrar</button>
</form>
