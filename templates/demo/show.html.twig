{% extends 'base.html.twig' %}

{% block title %}Demo{% endblock %}

{% block body %}
    <h1>Demo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ demo.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ demo.name }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ demo.email }}</td>
            </tr>
            <tr>
                <th>Ciudad</th>
                <td>{{ demo.city }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('demo_index') }}">Volver a la lista</a>

    <a href="{{ path('demo_edit', {'id': demo.id}) }}">editar</a>

    {{ include('demo/_delete_form.html.twig') }}
{% endblock %}
